# ============================================================
# Dossier_Management - Document Pipeline Dependencies
# Install: pip install -r requirements.txt
# ============================================================

# --- PDF parsing (text extraction + page screenshots) ---
PyMuPDF>=1.24.0

# --- Image processing (screenshot handling) ---
Pillow>=10.4.0

# --- Deck builder (SVG components -> PPTX) ---
python-pptx>=1.0.0

# --- Web API (HTTP interface) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# --- Document-format conversion (pptx/docx -> PDF via MS Office COM) ---
# Drives the locally-installed Microsoft Office (PowerPoint / Word) through
# COM automation. No external commercial license needed — reuses the
# Office license already owned. Requires Windows + interactive desktop session.
comtypes>=1.1.0

# --- OCR pre-pass (scanned / image-only PDFs) ---
# Scanned PDFs have no text layer, so the classify/ingest/denoise stages cannot
# read them. The pipeline OCRs such PDFs with easyocr to synthesize a text
# layer. The heavy engine (torch) is already provided by the project venv; this
# pulls the remaining deps (torchvision, opencv, etc.).
easyocr>=1.7.0
